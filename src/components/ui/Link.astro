---
import type { HTMLAttributes } from 'astro/types'

interface Props extends HTMLAttributes<'a'> {
  href: string | URL
}

const { href, ...rest }: Props = Astro.props
---

<a href={href} class="text-blue-500 hover:underline" {...rest}>
  <slot />
</a>
